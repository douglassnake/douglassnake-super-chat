services:
  api:
    environment:
      SECRET_BACKEND: files
      SECRET_DIR: /run/superchat-secrets
    volumes:
      - type: bind
        source: ${SUPERCHAT_SECRET_DIR_HOST:?set SUPERCHAT_SECRET_DIR_HOST to a private host directory}
        target: /run/superchat-secrets
        read_only: true
